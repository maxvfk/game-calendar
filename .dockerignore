.git
# .github and .gitea are NOT ignored: the test suite asserts on the refresh workflows, and
# the image build runs that suite as its gate. It reaches the build stage only —
# the runtime stage copies nothing but public/ and serve.ts.
.gitlab-ci.yml
node_modules
public
data
*.sqlite
.idea
docs
.claude
README.md
AGENTS.md
CLAUDE.md
